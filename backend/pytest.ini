[pytest]
# testpaths is load-bearing: a bare `pytest` run collects only the hermetic
# unit suite in tests/. The smoke/ suite hits the live prod API and is opted
# into explicitly (`pytest smoke`), so it never runs by accident in CI unit jobs.
testpaths = tests
addopts = -q
