__pycache__/

# Price caches and result frames: ~7.5MB, and every one of them is rebuilt by
# the scripts that wrote it (`--download` where a network pull is involved).
*.pkl
*.csv

# Archived constituent pages. 22 captures, only ~5 of which parse; the
# distilled result is committed as transitions.json instead.
snaps/

# Chart payloads and intermediate stats, regenerated by build_chartdata.py,
# index_vs_holdings.py and since_may.py.
chartdata_asof.json
indexline.json
gold_slide.json
vol_refresh.json
timeline.json
